export const generateUserDataForGoalSummary = (userid: string): string => {
  // Have

  return "";
};

export const generateUserDataForDailySummary = (userid: string): string => {
  // Have
  return "";
};

export const generateUserDataForWeeklySummary = (userid: string): string => {
  // Have

  return "";
};

export const generateUserDataForMonthlySummary = (userid: string): string => {
  // Have

  return "";
};
