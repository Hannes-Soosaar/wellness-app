// Risky route
Respond only in JSON, no plain text outside the JSON block

example:

Respond in JSON only. The JSON should have this structure:
{
  "text": "<The full plain text here>",
  "formatting": [
    { "type": "bold", "start": <number>, "end": <number> },
    { "type": "italic", "start": <number>, "end": <number> },
    { "type": "link", "start": <number>, "end": <number>, "url": "<the link>" }
  ]
}

Here is the user input: "Explain the Solar System with important words bolded and planet names italicized."


// 